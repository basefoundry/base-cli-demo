schema_version: 1

project:
  name: base-cli-demo
  languages:
    - python

python:
  manager: uv

test:
  command: ./tests/validate.sh

release:
  version_file: VERSION
  changelog: CHANGELOG.md
  tag_prefix: v
  github:
    repository: basefoundry/base-cli-demo
    release_title: "{repository} v{version}"
